# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# local build artifacts
*.zip
pristinegaze-server.zip

# claude code memory
.claude/

# claude / agent instruction files
CLAUDE.md
AGENTS.md
README.md

# typescript
*.tsbuildinfo
next-env.d.ts

# WP migration: member/order exports contain PII — never commit
members-export.json
all-subscribers-export.json
*-export.json
*.export.json
data-task.xlsx
*.xlsx
orders.json
~$*
.scratch/

# one-time / local-only scripts (not part of the app) — ignore new scripts
# going forward (already-committed scripts stay tracked).
scripts/
